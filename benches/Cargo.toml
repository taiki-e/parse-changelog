[package]
name = "bench"
version = "0.0.0"
edition = "2018"
publish = false

[dev-dependencies]
parse-changelog = { path = "..", default-features = false }

criterion = "0.3"

[[bench]]
name = "parse"
path = "parse.rs"
harness = false
